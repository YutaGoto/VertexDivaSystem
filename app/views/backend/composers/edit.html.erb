<p>編集</p>
<%= form_for :composer, url: backend_composer_path(@composer), method: :patch, html: {class: "pure-form"} do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit class: "pure-button pure-button-primary" %>
<% end %>