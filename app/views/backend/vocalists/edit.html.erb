<div class="container-fluid">
  <p>編集</p>
  <%= form_for :vocalist, url: backend_vocalist_path(@vocalist), method: :patch do |f| %>
    <%= render 'form', f: f %>
    <%= f.submit %>
  <% end %>
</div>