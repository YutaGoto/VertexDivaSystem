<p>編集</p>
<%= form_for :vocalist, url: backend_vocalist_path(@vocalist), method: :patch, html: {class: "pure-form"} do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit class: "pure-button pure-button-primary" %>
<% end %>
