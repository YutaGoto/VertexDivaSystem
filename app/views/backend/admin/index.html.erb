<table>
  <tr>
    <th>ID</th>
    <th>なまえ</th>
    <th></th>
  </tr>
  <% @admins.each do |admin| %>
    <tr>
      <td><%= admin.id %></td>
      <td><%= admin.name %></td>
      <td><%= link_to "show", backend_admin_path(admin.id) %></td>
    </tr>
  <% end %>
</table>
<%= link_to "new", new_backend_admin_path %>
