<p>編集</p>
<%= form_for :illustration, url: backend_illustration_path(@illustration), method: :patch, html: {class: "pure-form"} do |f| %>
  <%= render 'form', f: f %>
  <%= f.submit class: "pure-button pure-button-primary" %>
<% end %>